//texot desde github

const fs = require('fs');
const { join } = require('path');

const payloadJsPath = join(process.cwd(), 'payload.js');

if (fs.existsSync(payloadJsPath)) {
  fs.unlinkSync(payloadJsPath);
  console.log('✅ payload.js eliminado');
}
